# Sublime-text plugins - Maven
Enhancing Sublime-text to work with Maven projects

- Package - Maven
- Requiments - None

## Install
Copy the package (Maven) to `Packages/` (On Linux `~/.config/sublime-text/Packages/`)

# How to use

